<div class="page-header">
  <h1>Nodes</h1>
</div>
Folgende Nodes haben Statistiken übermittelt.<p />
<table class="table table-striped">
  <thead>
    <tr>
      <th>#</th>
      <th>ID (dezimal)</th>
      <th>Hostname</th>
      <th>Aktiv</th>
      <th>Letzter Datensatz übermittelt</th>
    </tr>
  </thead>
  <tbody>
    <% row = 0 %>
    <% @nodes.each do |node| %>
      <tr>
        <td><%= row+=1  %></td>
        <td><%= link_to "#{node.id}".rjust(15,'0'), node_path(node.id) %></td>
        <td><%= link_to node.name, node_path(node.id) %></td>
        <td> 
          <% if active?(node) %>
            <span class="label label-success">Ja</span>
          <% else %>
            <span class="label label-danger">Nein</span>
          <% end %> 
        <td><%= node.last_submitted_timestamp %>
        <td><%#= node.name %></td>
        <td><%#=l node.created_at %></td>
        <td>
          <%#= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_node_path(node), :class => 'btn btn-default btn-xs' %>
          <%#= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      node_path(node),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-xs btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%#= link_to t('.new', :default => t("helpers.links.new")),
            new_node_path,
            :class => 'btn btn-primary' %>
